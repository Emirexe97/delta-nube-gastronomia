{
  "name": "@gastronomy/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "dependencies": {
    "@gastronomy/application": "workspace:*",
    "@gastronomy/contracts": "workspace:*",
    "@gastronomy/domain": "workspace:*",
    "better-sqlite3-multiple-ciphers": "12.11.1",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.8.1",
    "tsx": "^4.19.1"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm rebuild better-sqlite3-multiple-ciphers && tsx --test src/**/*.test.ts",
    "benchmark": "pnpm rebuild better-sqlite3-multiple-ciphers && tsx src/benchmark.ts"
  }
}
